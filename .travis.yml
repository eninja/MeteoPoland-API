language: java
sudo: false # faster builds

addons:
  sonarcloud:
    organization: "eninja-github"
    token:
      secure: $SONAR_TOKEN

script:
  - 'mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent cobertura:cobertura install sonar:sonar'

after_success:
  - bash <(curl -s https://codecov.io/bash)